class Solution {
public:
    bool judgeSquareSum(int c) {
        double l=sqrt(c);
        for(int i=0;i<=int(l);i++){
            double a=sqrt(c-i*i);
            if(int(a)==a)
            return true;
        }
        return false;
    }
};
